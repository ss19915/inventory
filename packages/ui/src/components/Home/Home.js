import { Paper } from '@material-ui/core';
import DefaultView from '../Views';

const Home = (props)=> {
    return (
        <Paper>
            <DefaultView/>
        </Paper>
    );
};

export default Home;